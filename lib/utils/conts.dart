import 'package:flutter/material.dart';

abstract class AppBorder {
  static BorderRadiusGeometry segment = BorderRadius.circular(10.0);
  static BorderRadiusGeometry button = BorderRadius.circular(15.0);
  static BorderRadiusGeometry card = BorderRadius.circular(10.0);
}
